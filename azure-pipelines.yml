trigger:
- Dev

pool:
  vmImage: ubuntu-latest

variables:
  appServiceSku: 'S1'
  location: 'uksouth'
  resourceGroupName: 'rg-cloudplatformlab-dev'
  serviceConnection: 'sc-cloudplatformlab-dev'
  devEnvironment: 'env-cloudplatformlab-dev'

stages:

- stage: Build
  displayName: 'Build Application'
  jobs:
  - job: BuildApplication
    displayName: 'Build .NET Application'
    steps:
    - task: UseDotNet@2
      displayName: 'Install .NET 8 SDK'
      inputs:
        packageType: 'sdk'
        version: '8.x'

    - task: DotNetCoreCLI@2
      displayName: 'Restore'
      inputs:
        command: 'restore'
        projects: '**/*.sln'

    - task: DotNetCoreCLI@2
      displayName: 'Build'
      inputs:
        command: 'build'
        projects: '**/*.sln'
        arguments: '--configuration Release --no-restore'

- template: pipelines/templates/appservice.yml
  parameters:
    dependsOn: Build
    resourceGroupName: '$(resourceGroupName)'
    serviceConnection: '$(serviceConnection)'
    devEnvironment: '$(devEnvironment)'
    location: '$(location)'
    appServiceSku: '$(appServiceSku)'

- template: pipelines/templates/networking.yml
  parameters:
    dependsOn: Build
    resourceGroupName: '$(resourceGroupName)'
    serviceConnection: '$(serviceConnection)'
    devEnvironment: '$(devEnvironment)'

- template: pipelines/templates/observability.yml
  parameters:
    dependsOn: Build
    resourceGroupName: '$(resourceGroupName)'
    serviceConnection: '$(serviceConnection)'
    devEnvironment: '$(devEnvironment)'

- template: pipelines/templates/security.yml
  parameters:
    dependsOn: Build
    resourceGroupName: '$(resourceGroupName)'
    serviceConnection: '$(serviceConnection)'
    devEnvironment: '$(devEnvironment)'

- template: pipelines/templates/governance.yml
  parameters:
    dependsOn: Build
    resourceGroupName: '$(resourceGroupName)'
    serviceConnection: '$(serviceConnection)'
    devEnvironment: '$(devEnvironment)'
    location: '$(location)'

